---

---

<div
	class="announcement-bar group text-sm bg-black dark:bg-transparent dark:border-b dark:border-slate-800 hidden md:flex gap-1 overflow-visible px-3 py-2 relative text-ellipsis whitespace-nowrap cursor-pointer"
>
	<a
		target="_blank"
		rel="noopener"
		class="announcement-link ltr:ml-auto rtl:mr-auto ml-auto bg-contain inline-flex items-center transition-colors"
		title="Looking for early adopters! If you like Web Fragments, give us a star."
		href="https://github.com/web-fragments/web-fragments"
		style="color: #cbd5e1;"
	>
		<span>Do you love Web Fragments? Give us a&nbsp;</span>
		<span class="star-container relative inline-block" style="font-size: 1.1em;">
			<span
				class="flame absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-300 delay-200 pointer-events-none"
				aria-hidden="true">🔥</span
			>
			<span
				class="star-stamp relative inline-block group-hover:scale-125 transition-transform duration-300 ease-out z-10"
				>⭐️</span
			>
		</span>
	</a>
</div>

<style>
	.flame {
		animation: flicker 0.15s ease-in-out infinite alternate;
		filter: blur(1px);
		transform: scale(1.3);
	}

	@keyframes flicker {
		0% {
			opacity: 0.7;
			transform: scale(1.2) rotate(-5deg);
		}
		100% {
			opacity: 1;
			transform: scale(1.4) rotate(5deg);
		}
	}

	.group:hover .flame {
		animation: flicker 0.15s ease-in-out infinite alternate;
	}

	@media (prefers-reduced-motion: reduce) {
		.flame {
			animation: none;
		}
	}
</style>
